# Sample asks

A STRANGER OPENS IT — paste a failing setup and ask for a scored audit. Sub-bullets are notes — not the paste.

They enter (authored): A stranger describes the bot they're about to trust — what it does, who gets hurt when it quietly gets things wrong, and a few real messages it will face. The kit runs the eight tasks against those messages, reports pass or fail with the defense that flips each failure, and returns a go-live rule with a block number and a re-run trigger. Every example is instantiated from the builder's own bot — never leave a pack sample in the shipped files.

1. My failing setup: PeakWatch Ultra stopped logging after the firmware update — warranty or new purchase? Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Expect: A stranger describes the bot they're about to trust — what it does, who gets hurt when it quietly gets things wrong, and a few real messages it will face. The k…

2. My failing setup: You charged me twice for PeakWatch Ultra shipping; I need the second charge reversed today Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Expect: A stranger describes the bot they're about to trust — what it does, who gets hurt when it quietly gets things wrong, and a few real messages it will face. The k…

3. My failing setup: My PeakWatch Ultra band cracked on week two — repair, replace, or refund under the new policy? Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Expect: A stranger describes the bot they're about to trust — what it does, who gets hurt when it quietly gets things wrong, and a few real messages it will face. The k…

4. My failing setup: Cancel the PeakWatch Ultra renewal before Friday; keep the open warranty ticket #8821 Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Expect: A stranger describes the bot they're about to trust — what it does, who gets hurt when it quietly gets things wrong, and a few real messages it will face. The k…

5. My failing setup: Billing asked for proof of purchase but the warranty thread already has the receipt photo from Monday Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Expect: A stranger describes the bot they're about to trust — what it does, who gets hurt when it quietly gets things wrong, and a few real messages it will face. The k…

## How to score a try

- stranger: shell product audits their failing setup (not coaching them).
- Expect scored checks + severity + call + tripwire — never a coach question.
